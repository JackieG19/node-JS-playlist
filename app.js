
var fs = require('fs');

// asynchronous versions in Creating & Removing Directories

fs.mkdirSync('stuff');
// creates a folder(directory) called stuff

fs.rmdirSync('stuff');
// removes the folder(directory) called stuff



// fs.unlink('writeMe.txt'); 
// in terminal: node app = writeMe.txt is deleted

